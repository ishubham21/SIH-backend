SERVER_PORT=4000
POSTGRESQL_USER=admin
POSTGRESQL_PASSWORD=admin
POSTGRESQL_PORT=5432
JWT_ACCESS_TOKEN_SECRET="11:11"

# This was inserted by running `npx prisma init`:
# Environment variables declared in this file are automatically made available to Prisma.
# See the documentation for more detail: https://pris.ly/d/prisma-schema#accessing-environment-variables-from-the-schema

# Prisma supports the native connection string format for PostgreSQL, MySQL, SQLite, SQL Server, MongoDB and CockroachDB.
# See the documentation for all the connection string options: https://pris.ly/d/connection-strings

DATABASE_URL="postgres://admin:admin@localhost:5432"

# Prisma needs to have a showdow database to perform transactions on the DATABASE - This is only if we are using a hosted Postgres Database 
# If your Database is on heroku, you need to create two PSQL databases and put the URLs of both the DBs in DATABASE_URL & SHADOW_DATABASE_URL respectively
# You need not use this variable as long as we are not going live
SHADOW_DATABASE_URL=""